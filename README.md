# Real-Time Face & Smile Detector 🙂

A Computer Vision project built using Python and OpenCV.

## Features
* Detects faces in real-time using the webcam.
* Detects smiles *within* the face region to reduce errors.
* Highlights features with bounding boxes.

## How to Run
1. Install dependencies: `pip install opencv-python`
2. Run the script: `python main.py`

## Requirements
* Python 3.x
* OpenCV
